import React from 'react';
import { Layout } from './Layout';

interface RegisterProps {}

export const Register = ({}: RegisterProps): JSX.Element => {
  return (
    <Layout>
      <div>Register PATH</div>
    </Layout>
  );
};
